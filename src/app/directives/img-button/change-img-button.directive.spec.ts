import { ChangeImgButtonDirective } from './change-img-button.directive';

describe('ChangeImgButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeImgButtonDirective();
    expect(directive).toBeTruthy();
  });
});
